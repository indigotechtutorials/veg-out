<%= form_with url: chat_chat_messages_path(chat) do |f| %>
  <div class="flex flex-col">
    <%= f.label :message %>
    <%= f.text_area :message %>
    <br/>
    <%= f.submit "Send Message", class: "bg-green-500 text-green-100 p-2 rounded-lg cursor-pointer" %>
  </div>
<% end %>